const files = require('fs');
files.writeFileSync('hello.txt','Hi, this is my first program in node js');

// require -> mentions the specified module
// fs -> module that is used to access and manage modules
// files.writeFileSync -> used to create file and input data in file